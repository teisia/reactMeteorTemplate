import React from 'react'
import { Center, Page, VBox, Box } from 'react-layout-components';

export const Standings = React.createClass({
    render(){
      return(
        <h2>This is the Standings page</h2>
      )
    }
})
