import React from 'react'
import { Center, Page, VBox, Box } from 'react-layout-components';

export const MyPicks = React.createClass({
    render(){
      return(
        <h2>This is the My Picks Page</h2>
      )
    }
})
